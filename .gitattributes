# Normalise line endings so home ↔ work machines don't churn the diff.
* text=auto eol=lf

# Windows-native scripts keep CRLF.
*.ps1 text eol=crlf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.pdf binary
*.docx binary
