# QA-01 Evidence — 2026-07-29

สถานะ: **PARTIAL — ยังไม่ใช่ production-ready**

เอกสารนี้เป็นหลักฐานรอบ QA/UAT ที่รันจาก checkout ปัจจุบัน โดยแยกฐานข้อมูล
`sos_qa` สำหรับการทดสอบออกจาก local demo database เดิม และไม่แตะ production
หรือ secret จริง

## Fresh verification

| Gate | ผล | หลักฐาน |
|---|---|---|
| Unit | PASS | `pnpm test` — 299/299, 32 test files |
| TypeScript | PASS | `pnpm typecheck` — exit 0 |
| Lint | PASS | `pnpm lint` — exit 0 |
| Production build | PASS with warning | `pnpm build` — exit 0; มี NFT tracing warning จาก local filesystem storage |
| DB integration | PASS | `pnpm test:integration` บนฐานข้อมูล fresh `sos_qa` — 80/80, 16 test files |
| Runtime smoke | PASS | local `:3100`, `AUTH_MODE=internal`: assets 200/27, readiness overview 200/27, reports export 200 Thai CSV, `/today` 200, `/today/scan` 200, bootstrap 200 |

## Mandatory UAT cases

| Case | สถานะรอบนี้ | หลักฐาน / สิ่งที่ยังขาด |
|---|---|---|
| 1. 27 assets + baseline approval | PARTIAL | Seed มี 27 จุด และ baseline route integration ผ่าน; ยังไม่มีการอนุมัติจากผลสำรวจจริงครบ 27 จุด |
| 2. auto draft → edit/publish → assignment | PARTIAL | Schedule-batch และ assignment integration ผ่าน; ความหมายของ “auto draft” ใน spec ยังต้องยืนยันว่าอัตโนมัติจริงหรือ Planner เป็นผู้กดสร้าง |
| 3. QR/GPS/photo/pass → READY | PARTIAL | QR, GPS, attachment และ photo-required tests ผ่าน; ยังไม่มี browser camera UAT และ baseline/ผลสำรวจจริง |
| 4. critical fail → DOWN + Fault + WO + email | PARTIAL | Domain/API/integration และ SMTP mock ผ่าน; ยังไม่ได้ส่งผ่าน SMTP จริงด้วย environment ของเทศบาล |
| 5. repair/retest → Planner accept → READY | PARTIAL | repair route integration ผ่าน; ยังไม่มี live browser run ครบจน READY |
| 6. overdue → WATCH → UNKNOWN | PASS (automated) | readiness engine + scheduled recompute/job-tick integration ผ่าน; ยังไม่ได้รัน cron production จริง |
| 7. offline close/reopen → sync once | PARTIAL | queue unit tests ผ่าน; browser IndexedDB evidence เป็น manual-only ยังไม่มี automated browser test |
| 8. GPS >100m reason + review flag | PASS | integration tests ผ่านทั้ง reject เมื่อไม่มีเหตุผลและ accept เมื่อมีเหตุผล |
| 9. Executive read/export cannot mutate | PARTIAL | policy tests และ export endpoint read-only ผ่าน; ยังไม่ได้ทำ role-based browser UAT เต็มรูปแบบ |
| 10. Dashboard/PDF/Excel same filter | PARTIAL | dashboard metric source และ Thai CSV export ผ่าน; PDF เป็น print view และยังไม่มี automated report snapshot/visual comparison |
| 11. Cutover 27 poles | BLOCKED | เป็นงานปฏิบัติการ: ต้องสำรวจ อนุมัติ baseline และยืนยันสถานะเสาจริงทั้ง 27 จุด |

## Non-functional gates still open

- `AUTH_MODE=internal` เป็นข้อยกเว้น authentication ที่ต้องบันทึกและยอมรับใน release decision
- ยังไม่มี Playwright critical E2E, automated accessibility smoke และ report snapshot ใน CI (`QA-02`)
- มี backup script แต่ยังไม่มีหลักฐาน restore จริง (`OPS-01`)
- ยังไม่มีหลักฐาน SMTP จริง, browser offline UAT และ live repair → retest → READY

## Conclusion

ส่วน engineering ที่ตรวจซ้ำได้ผ่านครบ แต่ QA-01 ยังปิดไม่ได้จนกว่าจะได้หลักฐาน
จาก browser/mobile, SMTP, backup restore และการ cutover ข้อมูลจริง 27 จุด

